\documentclass[tikz,border=1mm]{standalone}

\providecommand{\tikzsetnextfilename}[1]{}

\tikzset{
  pipe dream cell/.style={draw=black!35, line width=0.35pt, fill=white},
  pipe dream dot/.style={font=\scriptsize, text=black!45},
  pipe dream cross/.style={font=\small\bfseries, text=black},
  pipe dream label/.style={font=\scriptsize, text=black!70}
}

% Draw a staircase-shaped pipe dream grid.
%
% Usage:
%   \pipeDreamStaircase[<tikz options>]{n}{row/col,row/col,...}
%
% The coordinate list gives the cells containing crosses.  Rows and columns are
% one-indexed, matching the notation D(a,\alpha)=\{(\alpha_j,a_j-\alpha_j+1)\}.
\newcommand{\pipeDreamStaircase}[3][]{%
  \begin{tikzpicture}[
      x=0.55cm,
      y=0.55cm,
      line cap=round,
      line join=round,
      #1
    ]
    \pgfmathtruncatemacro{\pdn}{#2}
    \foreach \row in {1,...,\pdn} {
      \pgfmathtruncatemacro{\pdlast}{\pdn-\row+1}
      \node[pipe dream label] at (-0.45,-\row+0.5) {\row};
      \foreach \col in {1,...,\pdlast} {
        \draw[pipe dream cell] (\col-1,-\row+1) rectangle (\col,-\row);
        \node[pipe dream dot] at (\col-0.5,-\row+0.5) {$\cdot$};
      }
    }
    \foreach \col in {1,...,\pdn} {
      \node[pipe dream label] at (\col-0.5,0.35) {\col};
    }
    \foreach \row/\col in {#3} {
      \node[pipe dream cross] at (\col-0.5,-\row+0.5) {$+$};
    }
  \end{tikzpicture}%
}

\begin{document}

\tikzsetnextfilename{pipe-dream-schubert-314652}
\pipeDreamStaircase{6}{1/1,1/2,1/5,2/2,3/2,5/1}

\end{document}
